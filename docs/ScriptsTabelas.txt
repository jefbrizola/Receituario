CREATE TABLE CLIENTE (
    CPF   VARCHAR(11) NOT NULL,
    NOME  VARCHAR(60)
);

ALTER TABLE CLIENTE ADD CONSTRAINT PK_CLIENTE PRIMARY KEY (CPF);

CREATE TABLE PEDIDO_CAB (
    STATUS       VARCHAR(1),
    PRECO_TOTAL  FLOAT,
    DT_EMISSAO   DATE,
    CLIENTE_CPF  VARCHAR(11),
    NUM_PEDIDO   INTEGER NOT NULL
);

ALTER TABLE PEDIDO_CAB ADD CONSTRAINT PK_PEDIDO_CAB PRIMARY KEY (NUM_PEDIDO);

CREATE TABLE PEDIDO_IT (
    QTD         NUMERIC(15,2),
    PRODUTO     INTEGER NOT NULL,
    PRECO_UNIT  FLOAT,
    NUM_PEDIDO  INTEGER NOT NULL
);

ALTER TABLE PEDIDO_IT ADD CONSTRAINT PK_PEDIDO_IT PRIMARY KEY (NUM_PEDIDO, PRODUTO);

CREATE TABLE PRODUTO (
    CODIGO     VARCHAR(10) NOT NULL,
    CONTROLE   VARCHAR(15),
    PRECO      FLOAT,
    DESCRICAO  VARCHAR(80)
);

ALTER TABLE PRODUTO ADD CONSTRAINT PK_PRODUTO PRIMARY KEY (CODIGO);

CREATE TABLE SOLI_RECEITA (
    STATUS       VARCHAR(1),
    COD_PRODUTO  VARCHAR(10) NOT NULL,
    NUM_PEDIDO   INTEGER NOT NULL
);

ALTER TABLE SOLI_RECEITA ADD CONSTRAINT PK_SOLI_RECEITA PRIMARY KEY (COD_PRODUTO, NUM_PEDIDO);

CREATE TABLE TEC_AGRICULA (
    NUM_REGISTRO  VARCHAR(20),
    CPF           VARCHAR(11) NOT NULL,
    NOME          VARCHAR(60)
);

ALTER TABLE TEC_AGRICULA ADD CONSTRAINT PK_TEC_AGRICULA PRIMARY KEY (CPF);




